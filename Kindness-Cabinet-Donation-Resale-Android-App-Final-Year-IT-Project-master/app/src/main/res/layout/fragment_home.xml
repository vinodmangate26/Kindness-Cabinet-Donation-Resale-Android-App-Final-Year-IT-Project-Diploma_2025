<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical"
    android:layout_marginTop="10dp">

    <!-- Search View -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/svCategoryFragmentSearchProductName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:queryHint="Search by Category and Product Name"
        app:iconifiedByDefault="false"
        android:background="@drawable/search_bg"
        android:layout_margin="10dp"
        android:textColor="@android:color/black"
        android:textColorHint="@android:color/darker_gray"
        app:iconTint="@android:color/black"/>

    <ListView
        android:id="@+id/lvDonateSaleCategoryFragmentShowMultipleCategory"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:nestedScrollingEnabled="true"/>

    <TextView
        android:id="@+id/tvDonateSaleCategoryFragmentNoCategoryAvailable"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="No Category Available"
        android:textAlignment="center"
        android:layout_gravity="center"
        android:textColor="#226DE1"
        android:textSize="20sp"
        android:textStyle="bold"
        android:fontFamily="@font/aldrich"
        android:visibility="gone"/>

</LinearLayout>